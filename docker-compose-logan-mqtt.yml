version: '3.7'
services:
    influx-ingester-logan-mqtt:
        build: 
            context: .
            args:
                config_file: './influx-ingester-logan-mqtt-config-for-compose.yaml'
        container_name: influx-ingester-logan-mqtt
        network_mode: host
